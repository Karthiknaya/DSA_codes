class Solution {
    public int countSeniors(String[] details) {
        int count=0;
        for(String word:details)
        {
           int a=word.charAt(11)-'0';
           int b=word.charAt(12)-'0';
           int c=a*10+b;
           if(c>60)
         count++;
        }
        return count;
    }
}
