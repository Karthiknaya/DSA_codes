class Solution {
    public void reverseString(char[] s) {
       int i=0;
       char[] arr=new char[s.length];
       for(int j=s.length-1;j>=0;j--){
           arr[i++]=s[j];
       }
       i=0;
       for(char item:arr){
           s[i++]=item;
       }
    }
}
